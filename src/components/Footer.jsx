import classes from '../styles/Footer.module.css';
function Footer(){
  return(
    <div className={`${classes.footer}`}>
      <p>All CopyRights saved in aybdev &copy;</p>
    </div>
  );
}
export default Footer;